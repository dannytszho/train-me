//import { useState } from "react";

const Aware = (props) => {
  return (
    <div>
      <h1 className="text-gray-200">{props.color}</h1>
      <h2>{props.shape}</h2>
    </div>
  );
};

export default Aware;
